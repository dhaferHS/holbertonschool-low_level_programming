#include<stdio.h>
#include<stdlib.h>
/**
 * main- program that prints the number of arguments passed into it
 *
 * @argc: argc
 * @argv: argv
 * Return: void
 */
int main(int argc, char **argv)
{
	int i, a, b, sum;
(void)argv;
if (argc !='\0')
{
	for (i = 0; i < argc; i++)
argc[1] = a;
argc[2] = b;
sum = a + b;
printf("%d\n", sum);
return (0);
}
else
{
		printf(Error);
	return (1);
}
}
