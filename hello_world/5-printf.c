#include <stdio.h>
/**
 * main - a fonction that prints a msg using printf
 * Return: (0) sucesses
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\x0A");
	return (0);
}
