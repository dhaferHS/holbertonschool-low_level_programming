#include "main.h"
#include <stdio.h>

/**
 * _islower - check the code.
 * @c: the character
 * Return: 1 in success otherwise 0
 */
int _islower(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
