#include "main.h"
#include <stdio.h>
/**
 * _isupper - check the code.
 * @c: the character
 * Return: 1 in success otherwise 0
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
